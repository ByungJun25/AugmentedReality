using UnityEngine;
using System.Collections;
using Windows.Kinect;

public class PptManager : MonoBehaviour {
/*
    public GameObject Left;
    private CollisionCheck L_colCheck;
    private bool Leftbool;
    private int LeftCount = 0;
    public int LeftEndCount = 500;

    public GameObject Right;
    private CollisionCheck R_colCheck;
    private bool Rightbool;
    private int RightCount = 0;
    public int RightEndCount = 500;


    // Use this for initialization
    void Start()
    {
        Leftbool = false;
        Rightbool = false;

        if (Left == null)
        {
            Debug.Log("Assign Game Object with Left Box");
        }
        else
        {
            L_colCheck = Left.GetComponent<CollisionCheck>();
        }

        if (Right == null)
        {
            Debug.Log("Assign Game Object with Right Box");
        }
        else
        {
            R_colCheck = Right.GetComponent<CollisionCheck>();
        }
    }

    // Update is called once per frame
    void Update()
    {
        if(LeftCount==LeftEndCount)
        {
            //왼쪽 버튼 실행 구문 입력
            LeftCount = 0;
            return;
        }
        if(RightCount==RightEndCount)
        {
            //오른쪽 버튼 실행구문 입력
            RightCount = 0;
            return;
        }
       
        if(L_colCheck.getCheck())
        {
            LeftCount++;
        }
        else
        {
            LeftCount = 0;
        }
        if(R_colCheck.getCheck())
        {
            RightCount++;
        }
        else
        {
            RightCount = 0;
        }

    }
    */
}
